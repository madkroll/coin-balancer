service: coin-balancer

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: eu-west-1
  memorySize: 128
  timeout: 10

  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "s3:PutObject"
      Resource: "arn:aws:s3:::coin-balancer/*"

package:
  include:
    - node_modules
    - services
    - handler.js
    - package.json

functions:
  poller:
    handler: handler.poll
    events:
      - schedule: rate(10 minutes)
